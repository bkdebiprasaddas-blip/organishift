# SESSION-2026-08-23-1.md — Technical log

**Objective:** Reset the project + git, wipe to a fresh scaffold, then ingest the
new frozen Phase 1 specs and align the rule book / agent to them.

## Env facts (verified, this session 2026-08-23)
- OS: Windows PowerShell shell. Repo dir `D:\Project\OrganiShift_MERN`.
- Node **v24.18.0**; npm **11.16.0**; git **2.55.0.windows.3** (all via
  `--version`).
- MongoDB **Server 8.3** installed at
  `C:\Program Files\MongoDB\Server\8.3\bin\mongod.exe`; Windows service
  `MongoDB` = **Running** (`Get-Service`); CLI not on PATH. Recorded in BOOTSTRAP.

## Append — continued prep (PLANNING phase)
- Verified environment (above) per §A.8; recorded REAL versions in BOOTSTRAP.
- Created remaining planning deliverables required by RULEBOOK §E:
  - `governance/planning/ARCH-DESIGN.md` — architecture + data design (5
    collections, indexes, tree design, clone, progress, authN/Z, topology, no-Phase-2).
  - `governance/planning/IMPL-SPEC.md` — implementation contract (layout,
    server/frontend conventions, validation, security, core service rules,
    testing standard, sprints).
  - `governance/planning/UI-SPEC.md` — six-page UI spec, states, TreeView contract.
  - `governance/documentation/SETUP-GUIDE.md` — install/run/configure/test/harden.
- PLANNING deliverable set now complete: PLAN, ARCH-DESIGN, IMPL-SPEC, UI-SPEC,
  TODO (+ SETUP-GUIDE in documentation/, BOOTSTRAP, SESSION, LOG).

## Decisions (approved)
- Owner approved a **full wipe** of git history (`Remove-Item .git`, re-init
  `-b v1`, remote re-added; nothing committed/pushed). Prior 8 commits gone.
- Owner approved wiping **all app code + all old planning/docs/logs**, keeping
  only the rule-book backbone: `AGENTS.md`, `governance/RULEBOOK.md`, `.gitignore`,
  empty `governance/` skeleton, `.claude/`, fresh `.git`. Done + verified.
- Owner approved **injecting Phase-1 specifics into `RULEBOOK.md`** (a recorded
  J0E exception to its portability rule) + aligning `AGENTS.md`.

## Work done (file-by-file)
- Wiped: `client/`, `server/` (incl. real `.env` — never read/logged),
  root `node_modules/`, `package.json`, `package-lock.json`, `eslint.config.js`,
  `OrganiShift.bat`, `.organishift-ports`, `Scratch/`, `README.md`, all old
  `governance/planning|documentation|ai-context(+archive)|work-log` content.
- Kept: `.gitignore`, `AGENTS.md`, `governance/RULEBOOK.md`, `.claude/`, `.git/`,
  empty `governance/` skeleton.
- New frozen specs added to ROOT by owner: `OrganiShift_Phase1_System_Design.docx`,
  `OrganiShift_AI_Agent_Execution_Spec.md`, `OrganiShift_Agent_Build_Spec.md`.
- `AGENTS.md`: replaced the stale "Project-specific" section with Phase 1
  content (3 roles, six pages, core workflow, new envelope, authoritative specs,
  no-Phase-2 guard).
- `governance/RULEBOOK.md`: added the owner-approved portability exception in the
  header note; appended **PART III — Project-Specific Alignment (P1–P15)**.
  All §A–§J and PART I/II preserved.
- Created fresh continuity: `governance/BOOTSTRAP.md`,
  `governance/planning/PLAN.md`, `governance/planning/TODO.md`.

## Artifacts cleaned / generated during discovery
- Temporarily generated `_extract_docx.ps1` and `Phase1_System_Design_extracted.txt`
  to read the .docx — **removed** after use.

## Blockers / notes
- Environment versions not yet recorded (deferred, §A.8).
- Remote `origin/v1` still holds the old history; next push replaces it — needs
  explicit approval. Nothing staged or committed.
- `OrganiShift_Agent_Build_Spec.md` at root is untracked and untouched; may be
  the legacy master build spec — pending owner call on keep vs. retire.

## Append 2 — ingest Agent_Build_Spec + reconcile planning docs (this turn)
- Read `OrganiShift_Agent_Build_Spec.md` **in full** (all 839 lines): it is the
  build contract — corrections C-1…C-11, source-defect fixes S-1…S-5, derived
  rules, T-001…T-037 task list, T-01…T-30 corrected acceptance tests.
- Key reconciliations applied to planning docs:
  - **Progress = child-average recursive** (leaf NOT_STARTED 0 / IN_PROGRESS 50
    / BLOCKED 0 / COMPLETED 100; parent = avg of immediate children; event =
    avg of roots), server-side only; leaf shows chip, never %.
  - **Field-gated** `PUT /api/event-items/:id` (status→owner/Manager/Admin
    leaf-only per transition table; assignee/priority/due→Manager only).
  - **Admin cannot assign** (Admin structure / Manager operations); logout = no
    endpoint; `/api/auth/me` exists; `GET /api/users` = Admin+Manager (S-1 fix).
  - **Ownership via inheritance** (§2.7): assignee on any node; own = self or
    ancestor assignee (via path); member cannot change assignee/priority/due nor
    re-open COMPLETED; members see owned + read-only ancestor rows.
  - **Corrected counts**: Food=7, Stage=4, plan=11, execution=13; itemCount=13.
  - **Stack additions**: Tailwind CSS, Lucide React, React Hook Form, Zod, axios.
  - **Named error codes**: VALIDATION_ERROR, CYCLE_DETECTED, NOT_A_LEAF,
    PLAN_NOT_FOUND, FORBIDDEN, INVALID_TRANSITION.
  - Dashboard = 6 frozen counters (leaf-only), Overdue overlaps (labelled),
    Blocked sub-line under Pending.
- Files updated: `AGENTS.md` (added build spec to authoritative), RULEBOOK PART
  III P1 (added build spec), `ARCH-DESIGN.md`, `IMPL-SPEC.md`, `UI-SPEC.md`,
  `PLAN.md`, `TODO.md`, `BOOTSTRAP.md`.

## Append 3 — mandatory secret scan + consistency QA (this turn)
- Per RULEBOOK scaffold DoD (§J6), scanned governance + root docs for accidental
  secrets (real passwords, JWT/DB credentials, API keys, bearer tokens,
  connection strings). **Result: clean.** Only benign doc references found:
  a local `mongodb://127.0.0.1:27017/organishift` host URI in SETUP-GUIDE and an
  illustrative `JWT_SECRET` env-var name in RULEBOOK — no real credentials.
- Confirmed `.env` / `server/.env` are git-ignored (via `git check-ignore -v`).
- Confirmed the three root spec files + `governance/BOOTSTRAP.md` are trackable
  (not ignored) — correct for frozen docs.
- Root tree tidy: `.claude/`, `.git/`, `governance/`, `.gitignore`, `AGENTS.md`,
  and the three spec files only. No stray artifacts.

## Append 4 — moved frozen specs to Scratch/archive (this turn)
- Per owner choice ("move files to scratch archive"), moved the three frozen spec
  files from ROOT to `Scratch/archive/`:
  `OrganiShift_Phase1_System_Design.docx`, `OrganiShift_AI_Agent_Execution_Spec.md`,
  `OrganiShift_Agent_Build_Spec.md`.
- Verified: all three present in `Scratch/archive/`; ROOT clean (only `.claude/`,
  `.git/`, `governance/`, `Scratch/`, `.gitignore`, `AGENTS.md`); git no longer
  lists the specs; `Scratch/` confirmed git-ignored (`git check-ignore`).
- **Impact:** the specs are now reference-only and git-ignored (not shipped/
  tracked). All their rules are already encoded in RULEBOOK PART III + planning
  docs, so the build contract remains recoverable; but a fresh clone would NOT
  include the original spec files. Noted in BOOTSTRAP.
- Temp move script removed.

## Next step
PLANNING prep complete; specs archived to `Scratch/archive/`. Await owner review.
To advance: **"approve plan"** → pass PLANNING. (Deletion decision: specs are now
archived, not deleted.)