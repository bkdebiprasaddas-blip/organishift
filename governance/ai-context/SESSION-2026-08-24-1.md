# SESSION-2026-08-24-1.md — Technical log

**Objective:** Owner asked to "check all documents and complete whatever is
required for Phase 1". Audit docs vs implementation, close functional gaps,
expand tests, refresh governance. Prior context: SESSION-2026-08-23-1.md
(planning, gates, UI iteration, initial M0–M6 build).

## Env facts
Same as 2026-08-23 (Node 24.18.0 / npm 11.16.0 / MongoDB 8.3 service Running).
New: **mongosh** present at `%LOCALAPPDATA%\Programs\mongosh`.

## Decisions (owner)
- **Stack stays JavaScript** ("ok keep js") after asking JS-vs-TS for a college
  project; the brief TS conversion start (server package.json/tsconfig/deps) was
  fully reverted and re-verified (5→ later 18 tests green).
- "Complete whatever required for Phase 1" = authorization to fix audited gaps.

## Audit → fixes (file-by-file)
1. **`server/src/controllers/eventController.js`**
   - GET `/events/:id` now `.populate('assigneeId','name role')` — execution page
     can render assignee names.
   - MEMBER scoped view rewritten: owned branch now includes **descendants** of
     an assigned node (spec §2.7 bug: previously ancestors only). Delegates to
     new shared util.
2. **`server/src/utils/scopeItemsForMember.js` (new)** — single implementation of
   member visibility (assigned node + descendants + ancestor rows); used by both
   event + dashboard controllers and tests.
3. **`server/src/controllers/dashboardController.js`**
   - Uses scope util; overdue now uses real `new Date()` (was hardcoded
     2026-08-23); added `myWork[]` (leaves assigned to caller w/ eventTitle,
     isOverdue, allowedTransitions per transition table incl. re-open rule) →
     feeds T-031 inline editing.
4. **`client/src/App.jsx`** — ProtectedRoute preserves attempted path via
   location.state.
5. **`client/src/pages/Login.jsx`** — redirects back to attempted route after
   login (T-010).
6. **`client/src/pages/Dashboard.jsx`** — full rewrite: skeleton loading, error +
   retry, six frozen counters, **My Assigned Work with inline status selects**
   driven by `allowedTransitions` (member re-open locked), upcoming events.
7. **`server/test/acceptance.test.js`** — expanded 5 → **18 tests**, all green:
   auth/bcrypt/JWT payload; RBAC FORBIDDEN (admin assigneeId, member priority,
   member re-open blocked / manager re-open allowed); tree (cycle detect no-op,
   valid move re-paths subtree, cascade delete no orphans, buildTree orphan-as-
   root, cross-scope parent rejected); clone defaults+isolation+itemCount;
   progress worked-example rounding (Procurement 67, event 64); NOT_A_LEAF;
   INVALID_TRANSITION; legal BLOCKED→IN_PROGRESS; member scoped-read util test.
8. Cleanup: deleted stray smoke-test event "annual function"; temp PS scripts
   removed from ROOT.

## Verification evidence
- `npm test` (server): **pass 18 / fail 0** on `organishift_test`.
- `npm run build` (client): clean (vite 5.4.21).
- API restarted after controller changes; live checks:
  - admin stats counters OK (`myWork` present).
  - manager assigned Vendor Quote Comparison → Priya (HIGH, due 2026-08-20).
  - Priya's GET `/events/:id` shows ONLY owned branch (Vendor Quotes + 2
    ancestors); her stats total=1, overdue=1, myWork[0].isOverdue=true,
    allowed=IN_PROGRESS/BLOCKED. ✔ §2.7 behaviour confirmed end-to-end.

## Not done / next
- Remaining Sprint 7/8 items: shared component extraction (T-013/T-018 note),
  HTTP-layer acceptance tests, full 13-step DoD rehearsal (needs TESTING gate
  trigger), deploy/rehearsal tasks T-038/T-039.
- Git: everything since `65f4c9d` is uncommitted by policy (owner triggers).

## Append 1 — "FIX ALL" closeout (same day, later)
Owner ordered full Phase-1 closure. Delivered:
- T-013: client/src/components/common/ (Toast+ToastProvider/useToast, Modal,
  ConfirmDialog, Spinner, SkeletonCard, EmptyState, ErrorState, ForbiddenNote);
  all 6 pages refactored onto them; ToastProvider mounted in App.
- T-018: single common/TreeView.jsx (render-prop recursion engine: isCollapsed/
  onToggle/renderMain/renderActions/renderCollapsed) now powers Library, Plan
  builder and Execution trees; per-page tree code deleted.
- T-033/T-034: Layout mobile drawer was dead (aside translated but no toggle) —
  added hamburger + backdrop + auto-close on nav; min-h-[44px] nav targets;
  toast live region, progressbar role, aria-labels on icon-only buttons.
- T-024: server/src/utils/withTx.js — session transactions with automatic
  no-session fallback on standalone Mongo; applied to planningService.
  deleteSubtree, eventService.scheduleFromPlan (+progress recalc threading),
  eventController.deleteExecutionItem cascade.
- T-037: new 	est/http.test.js (11 HTTP tests: envelopes, 401/403 route RBAC,
  Zod VALIDATION_ERROR details, CYCLE_DETECTED over HTTP, past-date schedule
  400, 404 JSON). Suite DB split (organishift_test_http) to stop cross-suite
  wipes; --test-force-exit needed because express-rate-limit holds a timer.
  TOTALS: 29/29 green.
- DoD: scripts/dod-rehearsal.js — 13-step scenario over real HTTP against
  fresh organishift_dod: **13/13 PASS** (Food 7 / Stage 4 / plan 11 /
  execution 13 / Procurement 67% / RBAC denials / counters add up).
  NOTE: two initial rehearsal FAILs were script counting bugs (double root
  count; ancestor-title predicate), server behaviour was correct — verified
  against raw DB before fixing assertions.
- Client build clean after every refactor.

## Next step
Phase-1 build + local verification COMPLETE. Awaiting owner: commit trigger,
TESTING gate acknowledgement, then T-038 deploy + T-039 rehearsal when wanted.