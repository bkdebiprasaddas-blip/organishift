# LOG-2026-08-24.md — Work log (plain English)

## Set out to do
You asked me to check all the documents and finish whatever was still missing
for Phase 1.

## What I found (audit)
Most of the app was already built and working. I found 4 real gaps:
1. The execution page couldn't show people's names (backend wasn't attaching
   them to the checklist).
2. Members could see a branch's heading but not the tasks inside it when work
   was assigned at branch level — the spec says they should see the whole
   assigned branch.
3. The Dashboard was missing the "My Assigned Work" panel with quick status
   buttons that the spec requires.
4. Overdue calculations used a frozen date instead of today's date, and if you
   refreshed on a deep page you got sent to the dashboard instead of back to
   where you were after logging in.

## What I did
- Fixed all four gaps in the backend and frontend.
- Gave members their own proper visibility rule in one shared place, used by
  both the event page and the dashboard.
- Expanded the automated test suite from 5 to 18 tests — all passing. They now
  cover login/password security, role permissions (who may assign what), tree
  safety rules (no cycles, clean deletes), blueprint cloning, the progress
  maths example from the spec, status transition rules, and member scoping.
- Rebuilt the website bundle cleanly and re-tested everything against the live
  servers: logged in as each role, assigned a task to Priya (member), and
  confirmed she sees only her own branch with an "overdue" flag — exactly as
  the spec describes.
- Cleaned up: removed one leftover test event and some temporary scripts.
- Updated the tracking documents (TODO checkboxes, BOOTSTRAP snapshot, this
  log, and the technical session file).

## Current state
Phase 1 is functionally complete end-to-end: plan blueprints → scheduling →
execution checklists → progress roll-up → dashboards, with all three roles
behaving per spec. What remains before we can call Phase 1 fully closed:
a handful of code-tidiness items (shared UI components), a full demo run-through
of the official 13-step scenario, and the deployment steps whenever you want
them.

## Next step
Say **"run tests"** when you want the formal TESTING gate pass (full DoD
rehearsal + remaining checks). Nothing has been committed to git — say the word
if you want that too.

## FIX ALL — Phase-1 closeout (latest)
You said **FIX ALL** on the remaining checklist. Everything is now done:
- All repeated UI pieces (toasts, modals, delete-confirmations, tree views)
  merged into one shared set used by every page — consistent look everywhere.
- The mobile menu button actually works now (before, phones had no way to
  open navigation), plus accessibility touches (screen-reader announcements,
  labels on icon buttons).
- Database multi-step operations (blueprint copies, deletions) are now wrapped
  in proper transactions where the database supports it, so a mid-way failure
  can't leave half-written data.
- Test suite grew to **29 automated tests — all passing** — including real
  HTTP requests checking error shapes and permission walls.
- Ran the official 13-step demo scenario start-to-finish against a scratch
  database: **all 13 steps passed** with the exact expected numbers
  (7 food items, 4 stage items, 11-item plan, 13-item execution, 67% branch).

**Status: Phase 1 build complete and locally proven.** Still untouched by
design: git commits (awaiting your go), deployment steps. Say the word for
either.
## Pushed & closed
- Everything committed as **a833d41** and pushed to GitHub (origin/v1). Repository clean.
- Phase 1 build + local verification officially complete. Next time: deployment (say "approve release").

## Session 2 (later same day) � final debug + Phase-1 close
- You asked me to debug and say what's next. I re-ran everything: **29/29
  automated tests pass, the website bundle builds clean, and the official
  13-step demo scenario passes 13/13.** Nothing is broken.
- Your decisions: testing results are accepted ("rest all OK"), and because
  this is a college project there will be **no deployment in Phase 1** � the
  app stays local (start it any time with start.bat in the project root).
- I recorded both decisions in the planning/continuity documents and closed
  out the task list accordingly.

**Status: Phase 1 is fully complete.** Nothing left to build or fix; no
commit was made this session (docs changed only, staged state untouched).
If you want anything next it's optional extras: college submission prep,
Phase-2 features, or just report a bug if you spot one.

## Session 2 append � improvement plan approved
You asked to improve Phase 1 and picked all four areas, letting me propose the
ideas. I audited everything (frontend, backend, docs) and found: a few real
backend gaps (one error type returns a raw 500 instead of a clean message;
about a dozen routes lack tests), some UI bugs (failed loads look like empty
lists, buttons allow double-clicks), zero examiner-facing documentation (no
README!), and assorted polish items. You approved ALL FOUR fix batches (no
dark mode) and the full list is now tracked in TODO.md as IMP-* items.
Nothing coded yet - waiting for your go-ahead.

## Session 2 append 2 � improvements implemented ('code it')
Built all four approved batches end-to-end:
- **Backend hardening:** every write endpoint now validates its input and
  returns clean error messages; a bad ID gives 400 (not 500); cascading deletes
  and the library-copy flow are transactional; copy logic moved out of the
  controller into a service.
- **Tests:** 11 new automated HTTP tests added -> suite now **40/40 green**
  (was 29).
- **Frontend fixes:** failed loads now show an error + Retry instead of fake
  empty lists; refreshes no longer flash skeletons; double-click protection on
  all mutation buttons; modals close with Escape and trap keyboard focus;
  empty titles are blocked with a visible hint; DONE/ONGOING events show the
  right colours; due dates readable on phones.
- **Polish:** one shared chip/badge style across pages, keyboard-operable row
  menus, clickable '+N more' on calendar days, friendlier relative due dates
  ('in 3 days'), contrast fixes, open plan is now bookmarkable (?plan=id).
- **Submission docs created from scratch:** README.md, SETUP.md, docs/
  API-DOCS.md, DATA-MODEL.md, DEMO-SCRIPT.md, VIVA-NOTES.md.

Final checks: 40/40 tests, production build clean, 13/13 DoD rehearsal passed.
Nothing committed yet (waiting for your word).

## Pushed & closed
- Improvement cycle committed as **632cfb2** and pushed to GitHub (origin/v1). Repository clean. Phase 1 + improvements officially complete.

