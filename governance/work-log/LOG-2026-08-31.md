# LOG-2026-08-31

## What I set out to do
- Check the whole app for bugs, feature gaps, and UI problems, and write them up so they can be fixed later one at a time without confusion.

## What I did
- Read every file in the app (all 21 client files and the entire server: routes, controllers, services, models, middleware, tests, scripts).
- Found **59 real, verified issues** — each confirmed against the actual code with file and line references (nothing guessed):
  - **12 serious** (6 client, 6 server) — examples: a dialog that yanks your cursor back while typing; the main "mark complete" checkbox always errors on fresh tasks; "Save" in the item-details popup silently creates duplicate items; the calendar hides the last days of some months (e.g. Aug 30–31, 2026) and shows events a day early for users in American timezones; missing permission checks that let a normal member rewrite shared planning data or edit other people's tasks; comment authorship can be faked; copying library items into plans silently loses their type, checklists, tags, and notes.
  - **21 medium** (fake "saved" indicators, no auto-logout when the session expires, keyboard/screen-reader gaps, attachment links that allow malicious URLs, no pagination, etc.)
  - **26 small** (polish and edge cases).
- Also explained why the current passing test suite (40/40) missed the serious permission bugs: the tests only tried the paths that were already guarded.
- Wrote one self-contained report: **`governance/documentation/BUG_AND_IMPROVEMENT_REPORT.md`** — every issue has its own checkbox, what's wrong, why it matters, exact fix steps, and how to verify the fix, plus a recommended fix order and the commands to re-run tests after each fix.

## Decisions
- **Report only — no code changed today.** Fixes will be done later, one by one, from the report's checkboxes (your choice).
- Two small product decisions are flagged inside the report for when we fix those items (how the complete-checkbox should behave, and what happens to scheduled events when a plan is deleted).

## Files changed
- Added: `governance/documentation/BUG_AND_IMPROVEMENT_REPORT.md` (the audit + fix checklist).
- Tidied: moved 3 older session logs to `governance/ai-context/archive/` (keeps only the latest 3, per house rules).

## Current state
- All 59 findings documented and ready to work through. No fixes applied yet — app behavior is exactly as it was (all 40 tests still passing as of 2026-08-26).

## Next steps
- When ready, say e.g. **"fix SV-H1"** (or any item ID from the report) and I'll fix just that one, with a regression test, and re-run the test suite + build before reporting done.
- Or, to move the project to release: **"approve release"**.
