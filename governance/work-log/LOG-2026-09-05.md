# Work Log — 2026-09-05

## What I set out to do
Finish a 14-item code-clarity cleanup that a prior session today started and
got interrupted on (partway through item 6 of 14). Not a bug hunt — just
making already-working code less confusing and less duplicated. The 14 items
came from a whole-codebase read-only review earlier today, which the user
approved fixing one by one.

## What I did
Recreated the 14-item to-do list (the tracking tool doesn't remember it
across a break), confirmed the first 5 items were already safely done, then
finished the remaining 9:

6. Removed a duplicated database query in the progress-calculation code — it
   was running the exact same "fetch and sort all items" query twice.
7. Sped up the progress calculation itself by remembering results it's
   already computed instead of recalculating the same subtree's progress
   over and over.
8. Combined three near-identical "don't let the last remaining admin get
   demoted or deactivated" checks into one reusable check.
9. Found that a "status change" rules table was written out three separate
   times (once on the server twice, once in the browser code) — looked
   closely and confirmed all three actually enforce the same rule, just
   written differently, so this was safe to combine into one shared file
   without changing any behavior.
10. Gave the "users" feature its own dedicated business-logic file, matching
    how every other feature (planning, events, event plans) is already
    organized — user-related code was the only one still mixed into the
    request-handling layer.
11. Made the "how do we save an edit" pattern consistent across three
    different features that were each doing it slightly differently.
12. There was one spot (in the front-end's API helper) where the server's
    "here's what just happened" message text gets silently thrown away.
    Checked every screen in the app and found they all already write their
    own success messages anyway, so nothing was actually broken — asked
    before touching it, and left the behavior as-is with an explanatory note
    added.
13. Made one part of the Planning Library page recompute a list only when
    the underlying data actually changes, instead of every single time the
    page re-renders.
14. Cleaned up a confusing bit of error-handling code in the
    transaction-helper utility so it's obvious at a glance that it can't
    ever crash from a missing value — no behavior change, just easier to
    read.

After every single change, re-ran the full automated test suite (48
tests) to make sure nothing broke — it passed every time. Also rebuilt the
front-end app once after the last couple of front-end edits to double-check
it still compiles cleanly.

## Decisions
- Item 9 (the "status rules written three times" item) looked, on the
  surface, like it might have a real bug hiding in it — the three places
  didn't look identical at first glance. Read all three carefully and
  confirmed they actually behave exactly the same in every case; it was just
  written three different ways, not three different rules. So this stayed a
  safe cleanup, not a behavior change.
- Item 12 (front-end throwing away the server's status message) was the one
  place with a real judgment call, so I asked before touching it. Decision:
  leave it as-is, just add a comment explaining why.

## Files changed
- `client/src/components/common/TaskDetailDrawer.jsx`
- `client/src/pages/EventExecution.jsx`
- `client/src/pages/PlanningLibrary.jsx`
- `client/src/services/api.js`
- `server/src/controllers/dashboardController.js`
- `server/src/controllers/eventController.js`
- `server/src/controllers/eventPlanController.js`
- `server/src/controllers/planningController.js`
- `server/src/controllers/userController.js`
- `server/src/models/User.js`
- `server/src/routes/userRoutes.js`
- `server/src/server.js`
- `server/src/services/eventPlanService.js`
- `server/src/services/eventService.js`
- `server/src/services/planningService.js` (new methods added)
- `server/src/services/progressService.js`
- `server/src/utils/withTx.js`
- `server/src/services/userService.js` — **new file**
- `server/src/utils/statusTransitions.js` — **new file**
- `server/src/utils/urlScheme.js` — new file (carried over from the earlier,
  interrupted part of this session)

## Current state
All 14 cleanup items are done and verified (48/48 automated tests pass,
front-end builds cleanly). Nothing was changed in how the app actually
behaves for a user — this was entirely internal code cleanup.

## Next steps
- Nothing pending from this cleanup list.
- Changes are NOT committed to git — waiting for the user to explicitly say
  so, per standing project policy.
- The front-end's automated linter still can't run (it was never fully set
  up in this project) — a pre-existing gap noted in yesterday's log, not
  something today's work touched or needs to fix.
